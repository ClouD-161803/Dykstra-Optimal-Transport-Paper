% When evaluating a dense cloud of $M$ particles, it becomes mathematically apparent that if particle $i$ and particle $i+1$ are spatially adjacent such that $z_1^{(i)} \approx z_1^{(i+1)}$, the corresponding row vectors ${A}_{i,:}$ and ${A}_{i+1,:}$ will be nearly identical in orientation. 

% During the execution of the Projected Gradient Descent algorithm, any unconstrained gradient step $\tilde{{w}}$ that violates the monotonicity condition (i.e., any combination of polynomial coefficients that attempts to ``fold'' the crescent domain over itself) will necessitate a projection onto the feasible space defined by $-{A}{w} \le -{\epsilon}$. Integrating the fast-forwarding step dynamically computes the requisite cyclic skips, bypassing the acute corners of the polyhedron and rendering the continuous optimal transport formulation significantly more tractable for practical filtering applications.